class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        n=len(nums1)
        nums2=set(nums2)
        res=set()
        for i in range(n):
            if nums1[i] in nums2:
                res.add(nums1[i])
        return list(res)
