class Solution(object):
    def sumOfUnique(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        freq=[0]*101
        for i in nums:
            freq[i]+=1
        
        return (sum(i for i in range(101) if freq[i]==1))
        
